version: "3.9"

services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./output:/output

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"

  llm:
    image: ollama/ollama
    ports:
      - "11434:11434"
